# Hello World

## {Introduction @unplugged}

Este é o meu primeiro tutorial no MakeCode Arcade.

## Step 1: Fundo
### Vamos só testar se o tutorial abre e funciona.

- Abra a categoria ``||scene:Scene||``.
- Pegue o bloco ``||scene:set background color to ()||``.
- Coloque o bloco dentro do ``||loops:on start||``.

```blocks
//@highlight
scene.setBackgroundColor(9)
```